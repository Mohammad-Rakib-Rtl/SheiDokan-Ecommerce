'use client';
import Image from 'next/image';import Link from 'next/link';import {Heart} from 'lucide-react';import {motion} from 'framer-motion';import {Product} from '@/lib/data';import {useCart} from '@/store/cart';
export function ProductCard({p,large=false}:{p:Product;large?:boolean}){const wish=useCart(s=>s.wish);return <motion.article initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true}} className={`product-card group ${large?'md:col-span-2 md:row-span-2':''}`}><Link href={`/product/${p.slug}`} className="block overflow-hidden bg-[#f6f6f4]"><div className={large?'aspect-[4/5]':'aspect-[3/4]'}><Image src={p.image} alt={p.name} width={900} height={1200} className="h-full w-full object-cover"/></div></Link><div className="mt-4 flex justify-between gap-4"><div><p className="text-[11px] font-bold uppercase tracking-[.16em]">{p.brand}</p><h3 className="mt-1 max-w-xs text-sm uppercase leading-5">{p.name}</h3><p className="mt-1 text-sm text-neutral-500">৳{p.price.toLocaleString()} {p.oldPrice&&<span className="ml-2 line-through">৳{p.oldPrice.toLocaleString()}</span>}</p></div><button onClick={()=>wish(p.id)} aria-label="Add to wishlist" className="opacity-60 transition group-hover:opacity-100"><Heart size={20}/></button></div></motion.article>}
